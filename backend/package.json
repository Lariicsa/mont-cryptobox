{
  "name": "cryptobox-server",
  "version": "1.0.0",
  "description": "Cryptobox Server, a few API endpoints & Websocket connections handler",
  "main": "server.js",
  "scripts": {
    "dev": "ENV=dev nodemon --watch src src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.18"
  }
}
